# Markz 桌面界面规范

## 使用场景

以长时间写作与阅读为主，采用 Operate 模式。文档占据主要空间，侧栏与 AI 面板可以收起。参考 MarkText 的安静布局，保持真实源码编辑。

## 视觉

- 深石墨色界面与低饱和薄荷绿强调色；浅色主题使用暖白编辑区与清晰灰绿边界。
- 系统 UI 字体与中文无衬线回退，源码采用 Cascadia Code 与中文无衬线回退。
- 常规操作至少 12px，正文 15px、源码 14px；避免用装饰性小标签充当内容层级。
- 内容按功能分区：顶部文件与模式，左侧结构索引，中间文档，按需显示右侧 AI。
- 命令面板提供低频动作；常用打开/保存/撤销可直接操作。

## 交互

保存、解析、生成状态分别来自真实执行结果。取消不提示成功。过期 AI 提案禁止应用。配置错误就地显示，编辑内容保留。

中文/英文切换不改文档。主题与语言本地保存。弹窗支持 Escape、焦点恢复与 Tab 循环；减少动画偏好生效。

主要实现：`src/renderer/styles.css`、`src/renderer/App.tsx`。
