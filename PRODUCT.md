# Markz

Markz 是 Electron + TypeScript 桌面 Markdown 编辑器与可独立集成的 SDK。

作者在本地写作，开发者可以复用编译与编辑能力。Markdown 原文是唯一真值；AST、预览、索引和 AI 提案均为派生数据。AI 只处理 Markdown 及其中的代码块。协作、账户、插件市场、移动端、复杂输出和完整 WYSIWYG 均不在范围。

正式范围以 `docs/01_需求/需求规格说明书.md` 为准；实际验证见验收报告。

平台：Windows Electron 桌面。默认中文，支持英文。无需 AI Key 即可使用编辑与导出。
